<h1 class="app-main-title">{{ translate('Mail') }} > {{ translate('Send scheduled') }}</h1>

<div class="alert alert-info">
  {{ translate('Mails scheduled to send were sent.') }}
</div>

<pre class="w-full p-2 border border-gray-200">{{ viewParams.log|join('\n') }}</pre>