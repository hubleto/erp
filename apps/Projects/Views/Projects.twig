<h1 class="app-main-title">{{ translate('Projects') }}</h1>

<app-projects-table-projects
  string:tag="table-projects"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></app-projects-table-projects>