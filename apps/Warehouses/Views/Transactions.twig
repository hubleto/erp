<h1 class="app-main-title">{{ translate('Warehouses') }} > Transactions</h1>

<app-warehouses-table-transactions
  string:tag="table-transactions"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></app-warehouses-table-transactions>