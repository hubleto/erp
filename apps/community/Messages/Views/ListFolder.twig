<h1 class="app-main-title">{{ translate('Messages') }}</h1>

<div class="flex gap-2 w-full">
  {% include '@HubletoApp:Community:Messages/FolderSelector.twig' %}
  <div>
    <div class="badge badge-extra-large ml-0">{{ viewParams.title }}</div>
    <div class="mt-4">
      <app-messages-list-folder string:folder="{{ viewParams.folder }}"></app-messages-list-folder>
    </div>
  </div>
</div>