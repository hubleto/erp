<h1 class="app-main-title">{{ translate('Mail') }} > Templates</h1>

<app-mail-table-templates
  int:record-id="{{ viewParams.recordId }}"
  bool:show-only-templates="true"
></app-mail-table-templates>
