<h1 class="app-main-title">{{ translate('Warehouses') }} > {{ translate('Transactions') }}</h1>

<hblreact-warehouses-table-transactions
  string:tag="table-transactions"
  int:record-id="{{ viewParams.recordId }}"
  int:direction="{{ viewParams.direction }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></hblreact-warehouses-table-transactions>