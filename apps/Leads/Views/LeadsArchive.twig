<h1 class="app-main-title">Archived leads</h1>

<app-leads-table-leads
  string:tag="table-leads"
  bool:show-archive="true"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></app-leads-table-leads>