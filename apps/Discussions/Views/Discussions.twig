<h1 class="app-main-title">{{ translate('Discussions') }} > {{ translate('Discussions') }}</h1>

<hblreact-discussions-table-discussions
  string:tag="table-discussions"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
  string:form-active-tab-uid='{{ viewParams.tab }}'
></hblreact-discussions-table-discussions>