<h1 class="app-main-title">{{ translate('Campaigns') }} > {{ translate('Recipients') }}</h1>

<hblreact-campaigns-table-recipients
  string:tag="table-recipients"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></hblreact-campaigns-table-recipients>