{#
  This is sample view generated by `php hubleto create` command
  For more details about customizing UI see:
    * https://developer.hubleto.eu/docs/views
    * https://developer.hubleto.eu/docs/ui
    * https://developer.hubleto.eu/advanced-development/customizing-ui
#}

<h1 class="app-main-title">{{ '{{' }} translate('{{ appName }}') {{ '}}' }} > {{ '{{' }} translate('{{ modelPluralForm }}') {{ '}}' }}</h1>

<hblreact-{{ appNameKebab|lower }}-table-{{ modelPluralFormKebab }}
  string:tag="table-{{ modelPluralFormKebab }}"
  int:record-id="{{ '{{' }} viewParams.recordId {{ '}}' }}"
  string:view="{{ '{{' }} viewParams.view {{ '}}' }}"
  json:filters='{{ '{{' }} viewParams.filters|json_encode {{ '}}' }}'
></hblreact-{{ appNameKebab|lower }}-table-{{ modelPluralFormKebab }}>