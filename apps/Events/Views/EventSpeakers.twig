<h1 class="app-main-title">{{ translate('Events') }} > {{ translate('Event speakers') }}</h1>

<hblreact-events-table-eventspeakers
  string:tag="table-eventspeakers"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
  string:form-active-tab-uid='{{ viewParams.tab }}'
></hblreact-events-table-eventspeakers>