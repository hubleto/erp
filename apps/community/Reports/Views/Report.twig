<style>
  canvas {
    width: 100%;
    height: 350px;
  }
</style>
<app-form-report
  config="{{ viewParams.report.getReportConfig()|json_encode }}"
  string:model="{{ viewParams.report.modelClass }}"
  string:name="{{ viewParams.report.name }}"
  bool:readonly="true"
></app-form-report>

