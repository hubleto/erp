<h1 class="app-main-title">{{ translate('Products') }}</h1>

<app-products-table-products
  string:tag="table-products"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></app-products-table-products>