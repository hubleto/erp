<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<style>
  * { font-family: "Helvetica"; font-size: 12px; }
</style>

<div>
  <div class="dtop">
    <div style="font-size:24pt"><b>Cenová ponuka</b></div>
    Deal: {{ identifier }} {{ title }}<br/>
    {% if version %} Verzia {{ version }}<br/> {% endif %}
    Vygenerovaná: {{ now }}<br/>
    Zákazník: {{ CUSTOMER.name }}<br/>
    Kontaktná osoba: {{ CONTACT.first_name }} {{ CONTACT.last_name }}<br/>
  </div>
  <br/>
  <br/>

  <table style="width:100%">
    <tr>
      <td style="width:60%"><b>Produkt</b></td>
      <td style="width:10%"><b>J. Cena</b></td>
      <td style="width:10%"><b>Množstvo</b></td>
      <td style="width:10%"><b>Zľava</b></td>
      <td style="width:10%"><b>Cena</b></td>
    </tr>
    {% for item in ITEMS %}
      <tr>
        <td style="width:60%">
          {{ item.PRODUCT.name }}
          {% if item.description %}
            <div style="color:#666666">
              {{ item.description }}
            </div>
          {% endif %}
        </td>
        <td style="width:10%">{{ item.unit_price|number_format(2, ",", " ") }} €</td>
        <td style="width:10%">{{ item.amount|number_format(2, ",", " ") }} </td>
        <td style="width:10%">{{ item.discount|number_format(0, ",", " ") }} %</td>
        <td style="width:10%"><b>{{ item.price_excl_vat }}|number_format(2, ",", " ") €</b></td>
      </tr>
    {% endfor %}
  </table>

  <div style="font-size:24px"><b>Cena: {{ price_excl_vat }}|number_format(2, ",", " ") €</b></div>

  Poznámka: Ceny nezahŕňajú DPH.
</div>
