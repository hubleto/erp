<h1 class="app-main-title">{{ translate('Worksheets') }} > {{ translate('Activities') }}</h1>

<hblreact-worksheets-table-activities
  string:tag="table-activities"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
  string:form-active-tab-uid='{{ viewParams.tab }}'
></hblreact-worksheets-table-activities>