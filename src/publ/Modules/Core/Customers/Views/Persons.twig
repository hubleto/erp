{% if viewParams.idAccount > 0%}
  <app-table-person
    string:endpoint="customers/persons-table"
    number:account="{{viewParams.idAccount}}"
  ></app-table-person>
{% else %}
  <app-table-person string:endpoint="customers/persons-table"></app-table-person>
{% endif %}