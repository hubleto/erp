<h1 class='app-main-title'>{{ translate('Invoices') }} > {{ translate('Items') }}</h1>

<app-invoices-table-items
  string:tag="invoices-table-items"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></app-invoices-table-items>