{% set a = viewParams.account %}

<h1 class="app-main-title">{{ translate('Mail') }} > Sent</h1>

<hblreact-mail-table-mails
  int:id-account="{{ a.id }}"
  bool:show-only-sent="true"
  bool:readonly="true"
  string:view="briefOverview"
></hblreact-mail-table-mails>
