<app-form-report
  configs="{{ viewParams.reportData|json_encode }}"
  string:model="{{ viewParams.reportData.model }}"
  string:name="{{ viewParams.reportData.name }}"
  bool:readonly="true"
  string:value="{{ viewParams.reportData.value }}"
  int:option="{{ viewParams.reportData.option }}"
></app-form-report>