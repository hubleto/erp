<h1 class="app-main-title">{{ translate('Warehouses') }} > {{ translate('Locations') }}</h1>

<hblreact-warehouses-table-locations
  string:tag="table-locations"
  int:record-id="{{ viewParams.recordId }}"
  json:filters='{{ viewParams.filters|json_encode }}'
></hblreact-warehouses-table-locations>