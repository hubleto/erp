<app-form-report
  config="{{ viewParams.reportConfig|json_encode }}"
  string:model="{{ viewParams.reportConfig.model }}"
  string:name="{{ viewParams.reportConfig.name }}"
  bool:readonly="true"
></app-form-report>

